# PostgreSQL connection string used by Prisma.
# Example:
# DATABASE_URL="postgresql://user:password@host:5432/dbname?schema=public"
DATABASE_URL="replace-me"

# Secret used to sign authentication JWTs.
# Generate a long, random value and store it in your secret manager.
JWT_SECRET="replace-me"

# HTTP port for the backend.
PORT=4000

# Secret used to encrypt sensitive data stored in the database.
# Required in production.
DATA_ENCRYPTION_KEY="replace-me"

# Resend API configuration used to send login notification emails.
# Create a verified sender/domain in Resend, then store your API key here.
RESEND_API_KEY="replace-me"
RESEND_FROM="Ant ID Training <no-reply@example.com>"

# Public URL of the frontend, used to build password reset links.
FRONTEND_URL="http://localhost:5173"

# Disable sending login notification emails. Set to `false` to avoid sending these emails on each login.
SEND_LOGIN_NOTIFICATION_EMAILS=true

# Logging level.
LOG_LEVEL="info"

# Allowed frontend origins for CORS.
# Example: CORS_ORIGINS="https://app.example.com,https://admin.example.com"
CORS_ORIGINS="replace-me"

# Redis URL for cache / rate limiting.
REDIS_URL="redis://localhost:6379"
